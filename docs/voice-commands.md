# Voice commands

Generated by `scripts/gen_commands.py` from `aliases.example.json`. Do not edit by hand.

123 phrases, 36 distinct actions. Say the wake phrase first,
for example "hey claude, latency report".

## Run by the listener (no model turn)

These never reach the assistant, so they cost no tokens and work while it is busy.

| Say | Runs or sends | What happens |
|---|---|---|
| "use the female voice", "use zira", "use the old voice", "use the classic voice" | `speak-response.mjs voice zira` | Switch the speaking voice to `zira`. |
| "use the male voice", "use david" | `speak-response.mjs voice david` | Switch the speaking voice to `david`. |
| "list voices" | `speak-response.mjs voices` | Read out the available voices. The output is read aloud. |
| "stop", "stop talking", "shut up", "be quiet", "quiet" | `speak-response.mjs stop` | Cut off speech that is playing now and release the speech floor. |
| "volume up", "louder", "speak up" | `speak-response.mjs volume up` | Raise the speech volume one step. |
| "volume down", "quieter", "turn it down" | `speak-response.mjs volume down` | Lower the speech volume one step. |
| "mute", "stop speaking to me", "voice off" | `speak-response.mjs disable` | Turn spoken replies off. |
| "unmute", "voice on", "start speaking to me" | `speak-response.mjs enable` | Turn spoken replies back on. |
| "cancel that", "never mind", "do not do anything", "do nothing", "do nothing at all", "don't do anything", "don't do anything at all", "don't do it", "just answer", "just respond", "nevermind", "no op", "noop", "ping", "test command", "testing" | `speak-response.mjs ping` | Do nothing. Proves the listener heard and matched the phrase; sends nothing to the assistant. The output is read aloud. |
| "gate off" | `speak-response.mjs gate off` | Disarm the start-of-turn approval gate. The output is read aloud. |
| "gate on" | `speak-response.mjs gate on` | Arm the start-of-turn approval gate. The output is read aloud. |
| "gate status" | `speak-response.mjs gate status` | Say whether the approval gate is armed. The output is read aloud. |
| "use aria", "use the neural voice", "use the new voice" | `speak-response.mjs voice aria` | Switch the speaking voice to `aria`. |
| "use jenny" | `speak-response.mjs voice jenny` | Switch the speaking voice to `jenny`. |
| "use emma" | `speak-response.mjs voice emma` | Switch the speaking voice to `emma`. |
| "use ava" | `speak-response.mjs voice ava` | Switch the speaking voice to `ava`. |
| "use guy" | `speak-response.mjs voice guy` | Switch the speaking voice to `guy`. |
| "use andrew" | `speak-response.mjs voice andrew` | Switch the speaking voice to `andrew`. |
| "use brian" | `speak-response.mjs voice brian` | Switch the speaking voice to `brian`. |
| "use christopher" | `speak-response.mjs voice christopher` | Switch the speaking voice to `christopher`. |
| "use sonia", "use the british voice" | `speak-response.mjs voice sonia` | Switch the speaking voice to `sonia`. |
| "use libby" | `speak-response.mjs voice libby` | Switch the speaking voice to `libby`. |
| "use ryan" | `speak-response.mjs voice ryan` | Switch the speaking voice to `ryan`. |
| "use natasha", "use the australian voice" | `speak-response.mjs voice natasha` | Switch the speaking voice to `natasha`. |
| "are you still working", "still working", "you still working", "are you busy", "are you done", "are you done yet", "you done yet", "are you finished", "is it done", "did you finish", "what are you doing", "what are you working on", "still there", "are you still there", "you still there", "are you still going", "status", "status check", "status report", "how's it going", "what's your status", "where are you at", "are you working on something" | `speak-response.mjs busy` | Say whether the assistant is working, waiting for approval, or idle. The output is read aloud. |
| "latency report", "how fast was that", "how long did that take", "timing report", "latency check" | `speak-response.mjs latency spoken` | Read out how long the last turn took at each stage. The output is read aloud. |
| "sound check", "mic check", "audio check", "check audio", "can you hear me", "are you listening", "hearing test" | `sound_check.mjs` | Read out microphone level and listener health. The output is read aloud. |
| "master status", "master status master status", "next master status", "master status interned", "first master status roll call", "master status roll call", "roll call" | `master_status.py` | Read out a status roll call across sessions. The output is read aloud. |

## Sent to the assistant as a prompt

The phrase is replaced by the exact text shown and submitted as a normal message.

| Say | Runs or sends | What happens |
|---|---|---|
| "renaissance fair mode", "renaissance fair", "renfaire mode" | `/masq:persona renfaire` | Send this text to the assistant as a prompt. |
| "plain english mode" | `/masq:persona plain` | Send this text to the assistant as a prompt. |
| "caveman mode" | `/masq:persona caveman` | Send this text to the assistant as a prompt. |
| "dean mode" | `/masq:persona dean` | Send this text to the assistant as a prompt. |
| "persona off", "normal mode" | `/masq:persona clear` | Send this text to the assistant as a prompt. |
| "persona status" | `/masq:persona status` | Send this text to the assistant as a prompt. |
| "go ahead", "proceed", "do it", "approved", "make it so", "green light", "yes go ahead", "go for it" | `Go ahead.` | Send this text to the assistant as a prompt. |
| "hold on", "wait", "hold that", "do not proceed", "stop the plan" | `Hold on. Do not proceed with that plan. Wait for new instructions.` | Send this text to the assistant as a prompt. |
